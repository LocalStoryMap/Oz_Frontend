/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'kr.object.ncloudstorage.com',
      'img1.kakaocdn.net',
      't1.kakaocdn.net',
    ],
  },
};

module.exports = nextConfig;
