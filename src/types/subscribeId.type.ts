export type Subscribe = {
  subscribeId: number;
  user: number;
  userEmail: string;
  impUid: string;
  merchantUid: string;
  isActive: boolean;
  startAt: string;
  expiresAt: string;
  isPaidUser: boolean;
};
